# Basic Tailwindcss starter

## install
npm install -D tailwindcss
npx tailwindcss init

## run
npx tailwindcss -i input.css -o output.css --watch

> Add to `~/.bash_profile` : `alias tailwind='npx tailwindcss -i input.css -o output.css --watch'`
